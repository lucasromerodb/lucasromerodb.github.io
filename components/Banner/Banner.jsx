import React from 'react';
import styled from 'styled-components';
import { Container, Pattern } from '../index'

const Box = styled.div`
    background-color: var(--blue200);
`

const Banner = () => {
    return(
        <Container hasPadding>
           
                Que onda pebet3
        </Container>
    )
}

export default Banner;